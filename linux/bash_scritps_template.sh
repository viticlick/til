#! /bin/bash

# Short description of the script

set -e errexit  # the script ends if a command fails
set -e pipefail # the script ends if a command fails in a pipeline
set -e nounset  # the script ends if it uses and undeclared variable
# set -o xtrace # if you want to debug
